# Google OAuth Configuration
# Path to your OAuth 2.0 client secret JSON file from Google Cloud Console
# Download from: https://console.cloud.google.com/apis/credentials
OAUTH_CLIENT_SECRET=./credentials/client-secrets.json

# Path where the OAuth token will be saved after authentication
OAUTH_CLIENT_TOKEN=./.gcp-saved-token.json

# Enable or disable logging to logs.txt
# Set to 'true' to enable logging, 'false' or leave unset to disable
ENABLE_LOGGING=false

